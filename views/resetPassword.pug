<!DOCTYPE html>
html(lang="en")
  head
    include partials/header
  body.login
    .contenedor-formulario
      h1= nombrePagina
      p Coloca tu Nueva Contraseña

      form.caja-login(method="POST")
        .campo
          label(for="password") Contraseña:
          input(type="password" name="password" id="password" placeholder="Nueva Contraseña" value= email )
        .campo.enviar
          input.boton(type="submit" value="Cambiar Contraseña")
        .campo.acciones
          a(href="/iniciar-sesion") Volver a Iniciar Sesión


    include partials/footer