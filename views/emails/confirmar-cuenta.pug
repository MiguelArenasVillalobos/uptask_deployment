<!DOCTYPE html>
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    style
      include ./email.css
  body
    h2 Confirma tu Cuenta UpTask
    p Ya estás casí inscrito, solo presiona el siguiente botón
    a.reset(href=`${confirmarUrl}`) Confirmar Cuenta
    p 
      | Si no puedes dar clic en Crear Cuenta puedes copiar este enlace: 
      a(href=`${confirmarUrl}`) #{confirmarUrl}
